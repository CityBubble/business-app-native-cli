import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    inputboxContainer: {
        width: 300,
        minHeight: 30,
        height: 50
    },
    Input: {
        color: '#fff',
        padding: 12,
        fontSize: 16,
    }
})

export default styles